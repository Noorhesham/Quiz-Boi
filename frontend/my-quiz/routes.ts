export const publicRoutes = ["/", "/login", "/signup","/users/:id"];
export const authRoutes = ["/login", "/signup"];
export const apiAuthPrefix = "/api/auth";
export const defaultLoginRedirect = "/";
